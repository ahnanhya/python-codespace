string=input("enter a string:")

#capitalizing the first letter string[0]
#and combining the rest of the letters string[1:]
capitalized=string[0].upper()+string[1:]

print(f"the string after capitalizing the first letter is {capitalized}")
